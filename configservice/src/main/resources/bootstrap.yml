server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pedcarmor/springcloudrepository.git
          searchPaths: config-data

  security:
    user:
      name: root
      password: root
        
        